module.exports = exports;


exports.compare = (hand1, hand2) => {
    // compare classes of hands

    // hand with higher
        // winning hand

    // same class
        // group sort and compare each sorted card
        // [2, 2, 3, 3, 3]
        // [2, 2, 4, 4, 4] // winner
}
